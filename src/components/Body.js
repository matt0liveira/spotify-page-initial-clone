import React from 'react'
import { BodyContainer } from '../AppStyle'

export default () => {
 return (
	 <BodyContainer>
		 <h1 className="body--title">Escutar muda tudo</h1>

		 <h4 className="body--subtitle">Milhões de músicas e podcasts para explorar. E nem precisa de cartão de crédito.</h4>

		 <button className="btn-primary">baixe spotify free</button>
	 </BodyContainer>
 )   
}